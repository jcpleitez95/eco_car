<h1 class="brand-name"><%= @brand.name%></h1>

<div class = "brand-model-container">
    <% @brand.models.each do |model| %>
        <div class="brand-model-tile">
        <%= link_to model.name, model_path(model)%>
        </div>
    <% end %>
</div>